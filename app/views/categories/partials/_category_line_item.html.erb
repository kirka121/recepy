<%= category.title.titleize %> <br/>
<%= category.description.to_s %> </br>
<%= link_to 'edit', edit_category_path(category), remote: true %>
<%= link_to 'delete', category_path(category), method: :delete, remote: true, data: { confirm: 'Are you sure?'} %>
<div class='form-modal form-edit' id="<%= category.id %>"></div>
<hr/>
<br/>